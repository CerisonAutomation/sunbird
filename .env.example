# Sunbird — Stripe configuration
#
# Sunbird ships with a real, production-path Stripe integration built on
# Stripe Payment Links (the officially recommended client-only checkout
# method now that stripe.redirectToCheckout has been removed). No backend
# is required to accept real payments:
#
#   1. In the Stripe Dashboard, create two Payment Links:
#        - "Sunbird Gold" as a one-time payment
#        - "Sunbird VIP" as a monthly subscription
#   2. Set each Payment Link's "After payment" confirmation page to redirect
#      to this app's URL with a marker query param, e.g.:
#        https://yourgame.example.com/?sunbird_stripe=gold
#        https://yourgame.example.com/?sunbird_stripe=vip
#      Sunbird detects this on load and grants the entitlement automatically.
#   3. Paste the Payment Link URLs and your publishable key below and
#      redeploy. Until these are set, Sunbird automatically runs its
#      polished sandbox "Demo Checkout" so the store is always fully
#      testable end-to-end.
#
# For real fulfillment security, pair this with a Stripe webhook
# (checkout.session.completed) that emails a license/redeem code — the
# in-app "Redeem code" box already accepts arbitrary promo codes, so a
# webhook-driven fulfillment flow can plug straight in.

VITE_STRIPE_PUBLISHABLE_KEY=
VITE_STRIPE_GOLD_LINK=
VITE_STRIPE_VIP_LINK=

# ------------------------------------------------------------- Portal builds
#
# Keep direct/PWA monetization in a direct build:
#   VITE_PORTAL_TARGET=none
#
# For a portal upload, build a dedicated artifact with *only* the portal SDK.
# This disables Sunbird's standalone Stripe / VIP / fake-ad surfaces at runtime
# and routes ad opportunities through the relevant approved provider:
#   VITE_PORTAL_TARGET=poki
#   VITE_PORTAL_TARGET=crazy
#
# CrazyGames can optionally supply a dashboard banner placement id. Leave blank
# unless the placement has been created and approved in their dashboard.
VITE_PORTAL_TARGET=none
VITE_CRAZY_BANNER_ID=

# ------------------------------------------------- Leaderboard & multiplayer
#
# Leave blank to run the on-device leaderboard (clearly labelled as such in the
# UI). Point it at any host implementing LEADERBOARD_API.md to go worldwide.
#
# A ready-to-deploy FREE serverless backend ships in ./backend (Cloudflare
# Workers + Durable Objects — 100k requests/day, hibernating WebSocket rooms,
# 5 GB SQLite leaderboard, all on the free plan):
#
#   cd backend && npm install && npx wrangler login && npx wrangler deploy
#
# then paste the printed workers.dev URL into both variables below
# (https:// for the leaderboard, wss:// for multiplayer) and rebuild.
# See backend/README.md for details and local dev (`npm run dev` → :8787).
VITE_LEADERBOARD_URL=
# e.g. VITE_LEADERBOARD_URL=https://sunbird-backend.<account>.workers.dev

# Optional WebSocket endpoint. When set, real players occupy Mass Race slots;
# unfilled slots stay as local squadron pilots.
VITE_MULTIPLAYER_URL=
# e.g. VITE_MULTIPLAYER_URL=wss://sunbird-backend.<account>.workers.dev
